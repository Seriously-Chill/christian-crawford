# Hero

One per page at most, only ever used on Home in the source. Headline in `display`, one tagline line under it in `body`/`muted`, then a primary and a secondary CTA side by side. The confirmed copy is "Christian Crawford – premium cleanliness" / "Innovations that take care of the cleanliness of your home every day" with CTAs "Get to know the range" (primary, → Product) and "Learn about the brand" (secondary, → About us) — swap the copy per page but keep the headline-tagline-two-CTA shape and the primary/secondary pairing (see `components/Button`).
